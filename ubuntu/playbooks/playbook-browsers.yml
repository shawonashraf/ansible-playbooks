---
- hosts: all
  become: true

  tasks:
    - name: Install Google Chrome
      # The .deb registers Google's apt repository on install, so later
      # system updates keep Chrome current.
      apt:
        deb: https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
