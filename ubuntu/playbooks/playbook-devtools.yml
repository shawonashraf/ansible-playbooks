---
- name: Install development tools
  hosts: all
  become: true

  tasks:
    - name: Install system development packages
      ansible.builtin.apt:
        name:
          - git
          - clang
          - llvm
          - cmake
          - make
          - build-essential
          - curl
          - libssl-dev
          - zlib1g-dev
          - libbz2-dev
          - libreadline-dev
          - libsqlite3-dev
          - libncurses-dev
          - liblzma-dev
          - tk-dev
          - libffi-dev
          - patch
          - python3-dev
          - zsh
          # git shells out to ssh-keygen -Y sign when gpg.format is ssh
          - openssh-client
          # deb822_repository needs it on the managed host
          - python3-debian
        state: present

    - name: Add the Docker repository
      ansible.builtin.deb822_repository:
        name: docker
        types: deb
        uris: https://download.docker.com/linux/ubuntu
        suites: "{{ ansible_distribution_release }}"
        components: stable
        architectures: "{{ 'arm64' if ansible_architecture == 'aarch64' else 'amd64' }}"
        signed_by: https://download.docker.com/linux/ubuntu/gpg

    - name: Install Docker Engine
      ansible.builtin.apt:
        update_cache: true
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-buildx-plugin
          - docker-compose-plugin
        state: present

    - name: Start and enable Docker service
      ansible.builtin.systemd:
        name: docker
        state: started
        enabled: true

    - name: Add user to docker group
      ansible.builtin.user:
        name: "{{ target_user }}"
        groups: docker
        append: true

    - name: Install Podman Compose
      ansible.builtin.apt:
        name: podman-compose
        state: present

    - name: Install gh cli
      ansible.builtin.apt:
        name: gh
        state: present

    - name: Add the VSCode repository
      ansible.builtin.deb822_repository:
        name: vscode
        types: deb
        uris: https://packages.microsoft.com/repos/code
        suites: stable
        components: main
        architectures:
          - amd64
          - arm64
        signed_by: https://packages.microsoft.com/keys/microsoft.asc

    - name: Install VScode
      ansible.builtin.apt:
        update_cache: true
        name: code
        state: present

    - name: Install nodejs
      # Unlike Fedora's, Ubuntu's nodejs package does not pull in npm.
      ansible.builtin.apt:
        name:
          - nodejs
          - npm
        state: present

- name: Install user development tools
  hosts: all
  become: false
  tasks:
    - name: Install Rust and Cargo
      ansible.builtin.shell: |
        set -o pipefail
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
        source $HOME/.cargo/env
      args:
        creates: "{{ target_user_home }}/.cargo/bin/rustc"
      # noqa: command-instead-of-module

    - name: Install pyenv
      ansible.builtin.shell: |
        set -o pipefail
        curl https://pyenv.run | bash
      args:
        creates: "{{ target_user_home }}/.pyenv/bin/pyenv"
      # noqa: command-instead-of-module

    - name: Configure shell for pyenv
      ansible.builtin.blockinfile:
        path: "{{ target_user_home }}/.bashrc"
        block: |
          export PYENV_ROOT="$HOME/.pyenv"
          export PATH="$PYENV_ROOT/bin:$PATH"
          eval "$(pyenv init --path)"
          eval "$(pyenv init -)"
          export PATH="$HOME/.cargo/bin:$PATH"
        marker: "# {mark} ANSIBLE MANAGED BLOCK - dev tools"
        create: true
        mode: "0644"

    - name: Install uv
      ansible.builtin.shell: |
        set -o pipefail
        curl -LsSf https://astral.sh/uv/install.sh | sh
      args:
        creates: "{{ target_user_home }}/.local/bin/uv"
      # noqa: command-instead-of-module

# Runs as target_user rather than the invoking account: git_config with
# scope: global writes to the executing user's ~/.gitconfig.
- name: Configure git identity and commit signing
  hosts: all
  become: true
  become_user: "{{ target_user }}"

  tasks:
    - name: Set the global git identity
      # Left untouched when the vault has no value, so a run without secrets
      # does not wipe an identity that is already configured.
      community.general.git_config:
        name: "{{ item.name }}"
        scope: global
        value: "{{ item.value }}"
      loop:
        - { name: "user.name", value: "{{ git_user_name }}" }
        - { name: "user.email", value: "{{ git_user_email }}" }
      loop_control:
        label: "{{ item.name }}"
      when: item.value | length > 0

    - name: Set up ssh commit signing
      # The key itself is created by playbook-github, which runs earlier.
      when: git_signing.enabled
      block:

        - name: Read the public key
          ansible.builtin.slurp:
            src: "{{ git_signing.key_path }}.pub"
          register: signing_pubkey

        - name: Configure git to sign with it
          community.general.git_config:
            name: "{{ item.name }}"
            scope: global
            value: "{{ item.value }}"
          loop:
            - { name: "gpg.format", value: "ssh" }
            - { name: "user.signingkey", value: "{{ git_signing.key_path }}.pub" }
            - { name: "commit.gpgsign", value: "true" }
            - { name: "tag.gpgsign", value: "true" }
            - { name: "gpg.ssh.allowedSignersFile", value: "{{ git_signing.key_path | dirname }}/allowed_signers" }
          loop_control:
            label: "{{ item.name }}"

        - name: Record the key as a trusted signer
          # Without this git log --show-signature reports every commit as
          # signed by an unknown key, including your own.
          ansible.builtin.copy:
            content: "{{ git_user_email }} {{ signing_pubkey.content | b64decode | trim }}\n"
            dest: "{{ git_signing.key_path | dirname }}/allowed_signers"
            mode: "0644"
          when: git_user_email | length > 0

        - name: Show how to register the key with GitHub
          # Commits sign locally either way, but GitHub marks them Verified
          # only once the public key is registered as a signing key, which is
          # separate from registering it for repository access.
          ansible.builtin.debug:
            msg:
              - "For commits to show as Verified, add {{ git_signing.key_path }}.pub"
              - "at https://github.com/settings/ssh/new with key type Signing Key:"
              - "{{ signing_pubkey.content | b64decode | trim }}"
