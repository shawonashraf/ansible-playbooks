---
- name: Install media playback and codec packages
  hosts: all
  become: true
  tasks:
    - name: Install the full codec set
      # Fedora's "dnf group install multimedia" equivalent; the rest of the
      # patented pieces come from ubuntu-restricted-extras in the starter.
      ansible.builtin.apt:
        name: libavcodec-extra
        state: present

    - name: Install VLC
      ansible.builtin.apt:
        name: vlc
        state: present

    - name: Install MPV
      ansible.builtin.apt:
        name: mpv
        state: present

    - name: Install handbrake
      # Ubuntu's "handbrake" is the GUI; the CLI is a separate package.
      ansible.builtin.apt:
        name:
          - handbrake
          - handbrake-cli
        state: present
